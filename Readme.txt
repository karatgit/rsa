Routing and Spectrum Allocation using the Dijkstra’s algorithm for the routing and
the First-Fit policy for the spectrum allocation.

The R files can be executed in the https://rstudio.cloud/
in the way that is shown in the rstudio_cloud image.